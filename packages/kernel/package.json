{
  "name": "@wofi/kernel",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && node --test dist/test/**/*.js",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"no lint configured for @wofi/kernel\""
  },
  "dependencies": {
    "@noble/ed25519": "^2.3.0",
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "typescript": "^5.3.3"
  }
}
